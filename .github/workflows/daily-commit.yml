name: Daily Commit

on:
  schedule:
    - cron: '30 2 * * *'  # runs daily at 2:30 UTC
  workflow_dispatch:

jobs:
  commit_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "23f1000280@ds.study.iitm.ac.in"

      - name: Create or update file to commit
        run: echo "Daily commit at $(date)" >> commit_log.txt

      - name: Commit changes
        run: |
          git add commit_log.txt
          git commit -m "Daily commit by 23f1000280@ds.study.iitm.ac.in" || echo "No changes to commit"

      - name: Push changes
        uses: ad-m/github-push-action@v0.6.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
